// TODO M2 check maximums of card abilities
// e.g. Doc Holliday sets maximum on bullets, or some abilitites can apply effect to some maximum
xdescribe('Maximums', function() {
    integration(function() {
        // card abilities that sets maximum for stat for all other effects
        describe('card abilities setting maximum', function() {
            beforeEach(function() {

            });

            it('should not allow the stat to pass maximum', function() {

            });

            describe('when the ability effect wears off', function() {
                beforeEach(function() {

                });

                it('should allow the card stat to pass maximum', function() {

                });
            });
        });

        // card abilities that can change the stat only up to maximum
        describe('card abilities with maximum', function() {
            beforeEach(function() {

            });

            it('should not allow the ability to change the stat past the maximum', function() {

            });
        });
    });
});
