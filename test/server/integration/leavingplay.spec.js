// TODO M2 not implemented
xdescribe('leaving play', function() {
    integration(function() {
        describe('when a lasting effect has been applied to the card', function() {
            beforeEach(function() {

            });

            it('should reset the lasting effect', function() {

            });
        });

        describe('when a card leaving play is saved', function() {
            beforeEach(function() {

            });

            it('should prompt to use the save and not have the card leave play immediately', function() {

            });

            it('should stop the card from leaving play once the ability is triggered', function() {

            });
        });

        describe('when a card ability has been used', function() {
            beforeEach(function() {

            });

            it('should reset after leaving play', function() {

            });
        });
    });
});
