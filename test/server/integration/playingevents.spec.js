// TODO M2 finish this test
xdescribe('playing actions', function() {
    integration(function() {
        beforeEach(function() {

        });

        describe('when playing an action', function() {
            beforeEach(function() {

            });

            it('should count as having played the action', function() {

            });
        });

        describe('when cancelling the effects of an action', function() {
            beforeEach(function() {

            });

            it('should not prompt to cancel the action again', function() {

            });

            it('should still count as having played the action', function() {

            });
        });

        describe('when an action becomes an attachment', function() {
            beforeEach(function() {

            });

            it('should count as playing an action', function() {

            });
        });
    });
});
